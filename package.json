{
  "name": "smart-socket-service",
  "version": "1.0.0",
  "description": "REST API service for controling and monitoring a smart socket. This service allows users to start and stop a smart socket, wait asynchronously for a set duration, receive real-time updates on power consumption, and fetch power usage statistics for specified time intervals. Integrates with MQTT for communication with smart socket devices.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "npm run build && node dist/app.js",
    "dev": "nodemon --watch src -e .ts --exec 'npm run build && node dist/app.js'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ylkhayat/smart-socket-service.git"
  },
  "author": "ylkhayat",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ylkhayat/smart-socket-service/issues"
  },
  "homepage": "https://github.com/ylkhayat/smart-socket-service#readme",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/morgan": "^1.9.9",
    "@types/shortid": "^0.0.32",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "mqtt": "^5.3.4",
    "shortid": "^2.2.16"
  }
}
